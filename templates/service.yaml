apiVersion: v1
kind: Service
metadata:
  name: {{ include "appman-demo.fullname" . }}
  labels:
    {{- include "appman-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "appman-demo.selectorLabels" . | nindent 4 }}
